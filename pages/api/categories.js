import axios from 'axios'
import { API_URL } from '@/config/index'

export default async (req, res) => {
  console.log(req)
  //const categories = await axios.get(`${API_URL}categories?select=slug,name`)
  //console.log('jhgklhgk ghkl', categories)
  // if (categories) {
  //   res.status(200).json(categories)
  // }
}
